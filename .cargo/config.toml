[build]
# Use v0 symbol mangling to prevent linker failures on macOS.
# Chumsky's deep monomorphization creates symbols exceeding the default
# mangling scheme's length limits.
rustflags = ["-C", "symbol-mangling-version=v0"]
