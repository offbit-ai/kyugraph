[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "kyugraph"
version = "0.1.0"
description = "Python bindings for KyuGraph embedded property graph database"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
